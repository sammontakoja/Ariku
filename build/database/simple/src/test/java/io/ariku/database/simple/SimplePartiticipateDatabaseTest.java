package io.ariku.database.simple;

import io.ariku.util.data.Competition;
import org.junit.Test;

import java.util.List;
import java.util.UUID;

import static org.hamcrest.CoreMatchers.is;
import static org.junit.Assert.*;

/**
 * @author Ari Aaltonen
 */
public class SimplePartiticipateDatabaseTest {

//    @Test
//    public void after_add_participation_it_can_be_found_from_competitions_list() {
//        SimplePartiticipateDatabase db = new SimplePartiticipateDatabase();
//        String userId = UUID.randomUUID().toString();
//        String competitionId = UUID.randomUUID().toString();
//        db.addParticipation(new Participation(userId, competitionId));
//        List<Competition> competitions = db.competitions(userId);
//        assertThat(competitions.get(0).id, is(competitionId));
//    }

}